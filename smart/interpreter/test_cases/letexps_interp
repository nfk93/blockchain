type storage = int

let%init storage = 17

let%entry main (p : int * string * nat) storage =
    let (i, s, n) = p in
    let a = 15 + i in
    let b = a + n in
        (([]: operation list), storage - b)

